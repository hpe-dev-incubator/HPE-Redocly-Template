import { OperationModel } from '../../redoc-lib';
import * as HAR from './har';
import { Languages } from './types';
export declare type CustomHAR = HAR.Request & {
    securityOAuth2ExtraCalls?: HAR.Request[];
    basicAuth?: {
        username: string;
        password: string;
    };
    pathParameters?: {
        [key in string]: string;
    };
};
export declare function getCodeSample({ lang, operation, exampleName, options, }: {
    lang: Languages;
    operation: OperationModel;
    exampleName?: string;
    options?: object;
}): string;
export declare function isDefined<T>(arg: T | undefined): arg is T;
export declare function capitalizeFirst(str: string): string;
