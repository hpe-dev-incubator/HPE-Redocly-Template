import * as React from 'react';
import { OperationModel, Server } from '../../redoc-lib';
export interface ServerDropdownProps {
    operation: OperationModel;
    onChange: (server: Server) => void;
}
export declare class ServerChooser extends React.Component<ServerDropdownProps, {
    activeIdx: number;
    variables?: {
        [name: string]: string;
    };
}> {
    state: {
        activeIdx: number;
        variables: {};
    };
    static getDerivedStateFromProps(props: any): {
        activeIdx: any;
    };
    handleChange: (option: any) => void;
    handleVariableChange: (name: string, value: string) => void;
    componentDidMount(): void;
    render(): JSX.Element;
}
export declare function expandVariables(url: string, variables?: object): string;
