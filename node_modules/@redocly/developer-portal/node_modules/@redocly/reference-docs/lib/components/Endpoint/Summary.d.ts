/// <reference types="react" />
import { OperationModel } from '../../redoc-lib';
import { PanelHeaderProps } from '../Panel';
export declare type EndpointSummaryProps = PanelHeaderProps & {
    compact?: boolean;
    operation: OperationModel;
    onToggleConsole: () => void;
    showConsole: boolean;
    tryItOverride?: {
        link?: string;
        label: string;
        onClick?: () => void;
    } | null;
};
export declare const Summary: ({ operation, compact, expanded, toggle, onToggleConsole, tryItOverride, showConsole, }: EndpointSummaryProps) => JSX.Element;
