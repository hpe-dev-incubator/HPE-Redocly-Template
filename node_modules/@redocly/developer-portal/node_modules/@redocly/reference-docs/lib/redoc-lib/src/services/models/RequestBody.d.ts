import { OpenAPIRequestBody, Referenced } from '../../types';
import { OpenAPIParser } from '../OpenAPIParser';
import { RedocProNormalizedOptions } from '../../../../services/ProStore';
import { MediaContentModel } from './MediaContent';
import { OperationModel } from './Operation';
export declare class RequestBodyModel {
    description: string;
    required: boolean;
    content?: MediaContentModel;
    constructor(parser: OpenAPIParser, infoOrRef: Referenced<OpenAPIRequestBody>, options: RedocProNormalizedOptions, { operation }?: {
        operation?: OperationModel;
    });
}
