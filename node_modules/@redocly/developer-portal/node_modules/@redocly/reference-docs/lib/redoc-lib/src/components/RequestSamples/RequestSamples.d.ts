/// <reference types="react" />
import { OperationModel } from '../../services';
export interface RequestSamplesProps {
    operation: OperationModel;
}
export declare const RequestSamples: ({ operation }: RequestSamplesProps) => JSX.Element | null;
