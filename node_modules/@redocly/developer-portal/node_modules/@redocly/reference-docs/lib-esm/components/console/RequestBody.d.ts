import * as React from 'react';
import { RequestBodyModel } from '../../redoc-lib';
import { Console } from './Console';
export interface RequestBodyProps {
    body: RequestBodyModel;
    resolvedBody?: any;
    validate: () => string | undefined;
    console: Console;
    id: string;
}
export declare class RequestBody extends React.Component<RequestBodyProps, {
    activeExampleIdx: number;
}> {
    state: {
        activeExampleIdx: number;
    };
    getActiveSample(idx: number): string;
    applyExample: (item: any) => void;
    encodeSample(sample: any): any;
    render(): JSX.Element;
    renderDropdown(props: any): JSX.Element;
}
