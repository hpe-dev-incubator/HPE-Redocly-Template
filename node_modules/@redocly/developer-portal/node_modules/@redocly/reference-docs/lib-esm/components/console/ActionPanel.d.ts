/// <reference types="react" />
import { OperationModel } from '../../redoc-lib';
export interface ActionPanelProps {
    execute: () => any;
    loading: boolean;
    operation: OperationModel;
    params: Record<string, string>;
    hasResponse: boolean;
}
export declare const ActionPanel: ({ loading, operation, hasResponse, execute, params, }: ActionPanelProps) => JSX.Element;
